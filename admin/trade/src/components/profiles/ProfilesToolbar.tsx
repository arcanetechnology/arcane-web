/** @format */

import { Toolbar } from '@mui/material';
import * as React from 'react';

const ProfilesToolbar: React.FC = () => {
  return <Toolbar>Profiles toolbar</Toolbar>;
};

export default ProfilesToolbar;
