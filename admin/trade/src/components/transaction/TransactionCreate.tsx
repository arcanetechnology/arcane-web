/** @format */

import * as React from 'react';
import { Button } from '@mui/material';

// forward ref
const TransactionCreate: React.FC = () => {
  return <Button variant="contained">Create Transaction</Button>;
};

export default TransactionCreate;
