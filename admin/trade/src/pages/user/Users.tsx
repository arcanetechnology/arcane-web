/** @format */

import * as React from 'react';

import { UserList } from '../../components';

const Users: React.FC = () => {
  return (
    <React.Fragment>
      <UserList />
    </React.Fragment>
  );
};

export default Users;
