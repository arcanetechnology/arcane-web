/** @format */

import { Typography } from '@mui/material';
import { Stack } from '@mui/system';
import * as React from 'react';

const ViewTransactions: React.FC = () => {
  return (
    <Stack>
      <Typography>Transaction view</Typography>
    </Stack>
  );
};

export default ViewTransactions;
