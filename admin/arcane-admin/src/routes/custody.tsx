/** @format */

import { Typography } from '@mui/material';
import { Stack } from '@mui/system';
import * as React from 'react';

const Custody: React.FC = () => {
  return (
    <Stack>
      <Typography>Custody Accounts Page</Typography>
    </Stack>
  );
};

export default Custody;
